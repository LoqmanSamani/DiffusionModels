version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - method: pip
      path: .  # This installs your torchdiff package
    - requirements: docs/requirements.txt  # Fixed path to match your file location